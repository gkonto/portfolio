services:
  - type: web
    name: gkonto-portfolio
    runtime: go
    region: frankfurt
    plan: free
    autoDeployTrigger: commit
    buildCommand: go build -tags netgo -ldflags '-s -w' -o app ./cmd/web
    startCommand: ./app
    healthCheckPath: /healthz
